import PageSection from "../PageSection";

const Recents = () => {
  return (
    <PageSection
      className="col-span-3"
      titleBar="Jump back in..."
    ></PageSection>
  );
};

export default Recents;
